import React from "react";
import { View, Text, Button } from 'react-native';

const ViewMetricsPage = ({ navigation }) => {
    return (
        <View>
            <Text>View Metrics Page!</Text>
        </View>
    );
}

export default ViewMetricsPage;